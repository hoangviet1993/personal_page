var char_list = []; // list of char on board
var word_list = []; // list of completed word :TBD
var tile_array = []; // list of all tiles that has word on the board(n,c,d)
var number_of_round = 0; // number of round: TBD
var deleted_tile;